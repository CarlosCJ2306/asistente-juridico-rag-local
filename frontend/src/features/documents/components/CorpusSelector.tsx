import { FormField, Select } from "../../../design-system";
import type { PublicDocument } from "../types";
interface CorpusSelectorProps { readonly documents: readonly PublicDocument[]; readonly documentId: string; readonly documentType: string; readonly knowledgeLayer: string; readonly onDocumentIdChange: (value: string) => void; readonly onDocumentTypeChange: (value: string) => void; readonly onKnowledgeLayerChange: (value: string) => void; }
export function CorpusSelector({ documents, documentId, documentType, knowledgeLayer, onDocumentIdChange, onDocumentTypeChange, onKnowledgeLayerChange }: CorpusSelectorProps) { const eligibleDocuments = documents.filter((document) => document.ragEligible); return <><FormField label="Documento específico">{(props) => <Select {...props} value={documentId} onChange={(event) => onDocumentIdChange(event.target.value)}><option value="">Todos los documentos elegibles</option>{eligibleDocuments.map((document) => <option key={document.id} value={document.id}>{document.displayName} · {document.knowledgeLayer}</option>)}</Select>}</FormField><FormField label="Tipo documental">{(props) => <Select {...props} value={documentType} onChange={(event) => onDocumentTypeChange(event.target.value)}><option value="">Todos los tipos</option><option value="expediente">Expediente</option><option value="normativa">Normativa</option><option value="jurisprudencia">Jurisprudencia</option><option value="otro">Otro</option></Select>}</FormField><FormField label="Capa documental">{(props) => <Select {...props} value={knowledgeLayer} onChange={(event) => onKnowledgeLayerChange(event.target.value)}><option value="">Todas las capas elegibles</option><option value="managed_corpus">Corpus administrado</option><option value="private_library">Biblioteca privada</option><option value="temporary">Temporal</option><option value="web_verified">Fuente web verificada</option></Select>}</FormField></>; }
